version: '2'
services:
  web:
    build: .
    #https://github.com/docker/compose/issues/123
    privileged: true
    container_name: manticore_local
    volumes:
      - './../../:/manticore'
      - /manticore/node_modules/
    ports:
      - "4555:4000"
      - "4646:4646" #nomad ui
    tty: true
